<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:diplom.Interface"
             x:Class="diplom.Views.TransactionInformationPage">

    <ContentPage.Resources>
        <local:TypeConverter x:Key="TypeConverter" />
        <local:CategoryConverter x:Key="CategoryConverter"/>
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Информация о транзакции:" FontSize="20" />
            <Label Text="{Binding Type, Converter={StaticResource TypeConverter}, StringFormat='Тип: {0}'}" FontSize="16" />
            <Label Text="{Binding Sum, StringFormat='Сумма: {0}'}" FontSize="16" />
            <Label Text="{Binding Date, StringFormat='Дата: {0:D}'}" FontSize="16" />
            <Label Text="{Binding CategoryId, Converter={StaticResource CategoryConverter}, StringFormat='Категория: {0}'}" FontSize="16" />
            <Button Text="Изменить" Clicked="EditButton_Clicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>